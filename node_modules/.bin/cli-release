#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../cli-engine/bin/cli-release" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../cli-engine/bin/cli-release" "$@"
  ret=$?
fi
exit $ret
